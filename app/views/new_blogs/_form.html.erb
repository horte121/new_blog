<%= form_for(new_blog) do |f| %>
  <% if new_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(new_blog.errors.count, "error") %> prohibited this new_blog from being saved:</h2>

      <ul>
      <% new_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_field :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
